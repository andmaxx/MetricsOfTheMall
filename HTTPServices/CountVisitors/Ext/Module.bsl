
Функция CV_APIGET(Запрос)
	Попытка
		ТекстHTML = Портал_СчетчикиПосещаемости.HTMLТекущиеДанныеПосещаемости();
		Ответ = Новый HTTPСервисОтвет(200);
		Результат = ТекстHTML;
	Исключение
		Ответ = Новый HTTPСервисОтвет(405);
	КонецПопытки;
	Ответ.УстановитьТелоИзСтроки(Результат,КодировкаТекста.UTF8);	
	Ответ.Заголовки.Вставить("Content-Type","text/html; charset=utf-8");
	Возврат Ответ;
	
КонецФункции
